# # package global variables
# geocodebr_env <- new.env(parent = emptyenv()) # nocov start
#
# .onLoad <- function(libname, pkgname){
#
#   # data release
#   geocodebr_env$data_release <- 'v0.3.0'
#
#   env$con <- con
#   env$db_path <- db_path
#   env$data_release <- data_release
#   env$cache_dir <- cache_dir
#
#
# } # nocov end
