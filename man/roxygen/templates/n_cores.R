#' @param n_cores Um número. O número de núcleos de CPU a serem utilizados no
#'   processamento dos dados. O padrão é `1`. Quando `n_cores = NULL`, o pacote
#'   utiliza o número máximo de cores físicos disponíveis.
